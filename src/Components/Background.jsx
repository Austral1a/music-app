import '../css/Background.scss'

import React, { Component } from 'react'

const Background = (props) => {
    return <div className="main-background">{props.player}</div>
}

export default Background
